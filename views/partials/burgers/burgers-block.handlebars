<li>
	

	<button class="change btn btn-sm btn-primary" data-id="{{item_id}}" data-newdev="{{devoured}}">
		{{#if devoured}}Devour{{else}}You ate this burger!{{/if}}
	</button>

	{{burger_name}}
</li>
